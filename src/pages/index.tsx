import { Fragment } from 'react';
import { HomeView } from '@/views/HomeView';

const HomePage = () => {
  return (
    <Fragment>
      <HomeView />
    </Fragment>
  );
};

export default HomePage;
