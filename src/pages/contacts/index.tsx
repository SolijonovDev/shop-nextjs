import React, { Fragment } from 'react';

import { ContactView } from '@/views/ContactView';

const ContactPage = () => {
  return (
    <Fragment>
      <ContactView />
    </Fragment>
  );
};

export default ContactPage;
